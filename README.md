# Rails5 Webpack React Crud Example

```
git clone

# install packages
bundle install
yarn install

# setup postgresql database
rake db:create
rake db:migrate
rake db:seed

# start
./bin/webpack-dev-server
rails s
```
